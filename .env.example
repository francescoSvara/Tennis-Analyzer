# Root environment variables example

# Backend / Server
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key_here
PORT=3001
NODE_ENV=development

# Bet Decisions Logging
# Set to true to log all bet decisions to bet_decisions table for audit
LOG_BET_DECISIONS=true

# Frontend (Vite) - public keys / base API URL
# Vite env variables must be prefixed with VITE_
VITE_API_URL=
VITE_SUPABASE_ANON_KEY=

# Notes:
# - Do NOT commit your real .env files. This file is an example only.
# - Copy this file to `.env` (root) and `backend/.env` if needed and fill with real values.
# - The backend requires SUPABASE_SERVICE_KEY (service role). The frontend can use VITE_SUPABASE_ANON_KEY if you want direct client access to Supabase.
